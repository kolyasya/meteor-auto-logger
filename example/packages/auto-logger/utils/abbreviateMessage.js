const abbreviateMessage = message => {
  const result = message;
  // result = result.replace(/organizations/g, 'org');
  // result = result.replace(/items/g, 'itm');
  // result = result.replace(/locations/g, 'loc');
  // result = result.replace(/available/g, 'avl');
  // result = result.replace(/marketplace/g, 'mkt');
  // result = result.replace(/inbound/g, 'in');
  // result = result.replace(/outbound/g, 'out');
  // result = result.replace(/inStock/g, 'stk');
  // result = result.replace(/users/g, 'usr');
  // result = result.replace(/labels/g, 'lbl');
  // result = result.replace(/counts/g, 'cnt');
  // result = result.replace(/types/g, 'typ');

  return result;
};

export default abbreviateMessage;
